/// <reference path="../ud2.js" />

// 日期选择控件
ud2.libExtend(function (inn, ud2) {
	'use strict';

	inn.controlCreater('auto', function (collection, constructor) {

		var // className存于变量
			cls = collection.className, cn = inn.className(cls);

		// 重写集合初始化方法
		collection.init = function (control) {

			

		};

	});

});